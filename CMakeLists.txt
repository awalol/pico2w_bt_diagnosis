# Generated Cmake Pico project file

cmake_minimum_required(VERSION 3.13)

set(CMAKE_C_STANDARD 11)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_EXPORT_COMPILE_COMMANDS ON)

# Initialise pico_sdk from installed location
# (note this can come from environment, CMake cache etc)

# == DO NOT EDIT THE FOLLOWING LINES for the Raspberry Pi Pico VS Code Extension to work ==
if(WIN32)
    set(USERHOME $ENV{USERPROFILE})
else()
    set(USERHOME $ENV{HOME})
endif()
set(sdkVersion 2.2.0)
set(toolchainVersion 14_2_Rel1)
set(picotoolVersion 2.2.0-a4)
set(picoVscode ${USERHOME}/.pico-sdk/cmake/pico-vscode.cmake)
if (EXISTS ${picoVscode})
    include(${picoVscode})
endif()
# ====================================================================================
set(PICO_BOARD pico2_w CACHE STRING "Board type")

# Pull in Raspberry Pi Pico SDK (must be before project)
include(pico_sdk_import.cmake)

project(pico2w_bt_diagnosis C CXX ASM)

# Initialise the Raspberry Pi Pico SDK
pico_sdk_init()

# Add executable. Default name is the project name, version 0.1

    add_executable(pico2w_bt_diagnosis
            hello_usb.c
            btstack_config.h
            )


    target_include_directories(pico2w_bt_diagnosis PRIVATE
            ${CMAKE_CURRENT_LIST_DIR}
            )

    target_compile_definitions(pico2w_bt_diagnosis PRIVATE
            CYW43_LWIP=0
            NDEBUG
            CYBT_ERROR_ENABLED=1
            CYBT_INFO_ENABLED=0
            CYBT_DEBUG_ENABLED=0
            )
    # pull in common dependencies
    target_link_libraries(pico2w_bt_diagnosis pico_stdlib)

    # enable usb output, disable uart output
    pico_enable_stdio_usb(pico2w_bt_diagnosis 1)
    pico_enable_stdio_uart(pico2w_bt_diagnosis 0)

    # create map/bin/hex/uf2 file etc.
    pico_add_extra_outputs(pico2w_bt_diagnosis)

target_link_libraries(pico2w_bt_diagnosis
    pico_btstack_classic
    pico_cyw43_arch_threadsafe_background
    pico_btstack_cyw43
)

